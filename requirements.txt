# ── Core PyTorch (CUDA 12.1 — matches Colab T4 as of 2025) ──────────────────
# On Colab, run this FIRST in a cell before pip install -r requirements.txt:
#   !pip install torch==2.2.0 torchvision==0.17.0 --index-url https://download.pytorch.org/whl/cu121
# On local CPU-only machine, run instead:
#   !pip install torch==2.2.0 torchvision==0.17.0 --index-url https://download.pytorch.org/whl/cpu
torch==2.2.0
torchvision==0.17.0

# ── HuggingFace ecosystem ────────────────────────────────────────────────────
transformers==4.44.2
huggingface_hub==0.24.6
datasets==2.18.0
evaluate==0.4.1
accelerate==0.27.2
tokenizers==0.15.2
sentencepiece==0.2.0

# ── Protobuf: pin to avoid conflict with transformers + sentencepiece ────────
protobuf==4.25.3

# ── NER evaluation ───────────────────────────────────────────────────────────
seqeval==1.2.2

# ── Scikit-learn ecosystem ───────────────────────────────────────────────────
scikit-learn==1.4.1.post1
pandas==2.2.1
numpy==1.26.4
scipy==1.12.0

# ── Hyperparameter optimization ──────────────────────────────────────────────
optuna==3.5.0
optuna-integration==3.6.0

# ── Interpretability ─────────────────────────────────────────────────────────
shap==0.44.1

# ── Visualization ────────────────────────────────────────────────────────────
matplotlib==3.8.3
seaborn==0.13.2

# ── Deployment ───────────────────────────────────────────────────────────────
streamlit==1.32.0
joblib==1.3.2

# ── Utilities ────────────────────────────────────────────────────────────────
pyyaml==6.0.1
tqdm==4.66.2
python-dotenv==1.0.1
